
In [1]: import pandas as pd

In [2]: d1 = pd.DataFrame({"lkey" : ["b","b","c","a","a"]
       ⋮ , "data1" : pd.Series(range(5), dtype="int64")})

In [3]: d2 = pd.DataFrame({"Rkey" : ["b","a"], "data2" :
       ⋮ pd.Series(range(2), dtype="int64")})

In [4]: pd.merge(d1,d2, left_on="lkey", right_on="Rkey")
Out[5]:
  lkey  data1 Rkey  data2
0    b      0    b      0
1    b      1    b      0
2    a      3    a      1
3    a      4    a      1

In [6]: pd.merge(d1,d2, left_on="lkey", right_on="Rkey",
       ⋮ how="outer")
Out[7]:
  lkey  data1 Rkey  data2
0    a      3    a    1.0
1    a      4    a    1.0
2    b      0    b    0.0
3    b      1    b    0.0
4    c      2  NaN    NaN

In [8]:
